#pragma once

#include <string>
#include <vector>

void compDirectories(const std::string& dirA, const std::string& dirB);
std::vector<std::string> listFiles(const std::string& dir);
std::vector<std::string> listDirectories(const std::string& directoryPath);

